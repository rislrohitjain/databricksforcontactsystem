# Antigravity Task: Build Databricks Contact Intelligence System

Create an end-to-end Python enterprise application in a project workspace named `databricksforcontactsystem`. The system integrates local PostgreSQL 18 with a Databricks SQL Lakehouse, includes high-density global contact records, features a production-ready dark-themed pure Python analytics dashboard, and scaffolds an extensible agentic layer for future LLM integration.

---

## 1. Project Specifications & Developer Profile

- **Directory:** `databricksforcontactsystem`
- **Lead Engineer Attribution:** Rohit Jain (Sr. Software Engineer & AI Automation Architect)
- **Live Portfolio Link:** https://rohitjain-resume.vercel.app/ (Embed prominently in dashboard header, sidebar, and footer)
- **Primary Runtime:** Python 3.11+
- **Local Relational DB:** PostgreSQL 18 (`localhost:5432`)
- **Cloud Warehouse:** Databricks SQL Warehouse
- **UI Engine:** Streamlit (Pure Python, responsive dark-mode theme, zero client-side external JS)
- **Architecture:** Decoupled Agent-Ready Engine (Tools for local DB + Databricks SQL queries)

---

## 2. Configuration & Credentials

Generate `.env.example`, `.env`, and load variables using `pydantic-settings` or `python-dotenv`:

```ini
APP_NAME=DatabricksContactSystem
HOST=0.0.0.0
PORT=8501

# PostgreSQL 18
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=databricksforcontactsystem

# Databricks Lakehouse
DATABRICKS_SERVER_HOSTNAME=dbc-e68b8705-9d03.cloud.databricks.com
DATABRICKS_HTTP_PATH=/sql/1.0/endpoints/YOUR_WAREHOUSE_HTTP_PATH
DATABRICKS_ACCESS_TOKEN=YOUR_DATABRICKS_ACCESS_TOKEN
DATABRICKS_CATALOG=hive_metastore
DATABRICKS_SCHEMA=default

# Profile
DEV_NAME="Rohit Jain"
DEV_ROLE="Sr. Software Engineer"
DEV_URL="[https://rohitjain-resume.vercel.app/](https://rohitjain-resume.vercel.app/)"